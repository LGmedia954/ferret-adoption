<%= render 'errors' %>

<%= form_for([@owner, @opportunity]) do |f| %>

  <i class='fas fa-heart' style='color:crimson'></i> <b>Interested? Click below to begin the screening process.</b> <i class='fas fa-heart' style='color:crimson'></i>
  
  <%= f.hidden_field :owner_id, value: session[:owner_id] %>
  <br><br><br>
  <%= f.submit "Submit" %>
<% end %>