<h1><%= @owner.first_name %> has a Busyness of <%= @owner.quantity %> Ferrets</h1>

<div>
  <% @owner.ferrets.each do |ferret| %>
    <div><%= link_to ferret.name, owner_path(ferret.name) %></div>
    Need to test this.
  <% end %>
</div>